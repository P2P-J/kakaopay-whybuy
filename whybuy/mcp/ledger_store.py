"""논거 원장 접근 단일 진입점 (0.7-3) — whybuy.

reasons.json 읽기·쓰기·스키마 검증은 이 모듈만 거친다. 오늘은 JSON 파일,
고객 서비스 전환 시 DB — 이 모듈 하나만 교체하면 된다. schema_version이
마이그레이션 앵커. 스킬·판정 엔진은 저장 방식을 모른다.

TODO(G3): 원장 스키마 + 격리 접근 구현. G0 스켈레톤.
"""
