# whybuy 환경 변수 예시 — 이 파일을 .env로 복사해 값을 채운다 (.env는 커밋 금지)
# OpenDART 무료 인증키 (https://opendart.fss.or.kr). 코드·로그·커밋에 노출 금지.
DART_API_KEY=
# fixture(기본, 오프라인) | live(.env 키 존재 시에만 API 호출)
WHYBUY_MODE=fixture
